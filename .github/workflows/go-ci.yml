name: Go CI

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.21.4' 

      - name: Install golangci-lint
        run: go get -u github.com/golangci/golangci-lint/cmd/golangci-lint

      - name: Run Tests
        run: go test -v ./...

      - name: Build
        run: go build -o myapp

  build-docker-image:
    runs-on: ubuntu-latest

    needs: build

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Docker
      uses: docker/setup-buildx-action@v1

      - name: Tag Existing Docker Image
        run: docker tag 20170044/gaza-runimage:latest 20170044/gaza-runimage:$(date '+%Y%m%d%H%M%S')
      
      - name: Build Docker Image
        run: docker build -t 20170044/gaza-runimage:latest .
      
      - name: Log in to Docker Registry
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          registry: 20170044

      - name: Push Docker Image
        run: docker push 20170044/gaza-runimage:latest

